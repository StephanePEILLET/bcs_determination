seed: 42
model_name: resnet50
num_classes: 120
lr: 0.001
optimizer_name: adam
scheduler_config:
  name: cosine_annealing
  T_max: 20
  monitor: val_loss
data_dir: /home/SPeillet/Downloads/stanford_dogs/images
image_size: 224
batch_size: 32
num_workers: 8
max_epochs: 1
patience: 5
log_every_n_steps: 10
val_check_interval: 1.0
precision: 16-mixed
use_tensorboard: true
use_wandb: false
wandb_project: stanford_dogs_classification
trainer:
  accelerator: cpu
  devices: 1
  profiler: null
  resume_from_checkpoint: null
inference:
  checkpoint_path: null
  batch_size: 64
  save_results: true
n_trials: 10
n_epochs_per_trial: 3
timeout: null
